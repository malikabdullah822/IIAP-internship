# Week 3 - Day 2: Arrow Functions & Semantic Structure

## Topics Covered
* **Arrow Functions:** Implemented modern ES6 arrow function syntax (`=>`) to replace traditional function declarations.
* **Semantic HTML Structure:** Designed the web page using HTML5 semantic elements (`<header>`, `<main>`, `<footer>`).
* **Arrow Functions vs Regular Functions:** Explored lexical `this` binding concepts and scoping differences.
* **Callbacks & Array Methods:** Utilized arrow functions inside event listeners (`addEventListener`) and array iteration (`forEach`).
* **Project Refactoring:** Converted earlier regular function logic into clean, modern arrow functions.

## Implementation Summary
* Developed an interactive Islamabad International Airport flight status portal.
* Built a dynamic `generateFlightMessage` arrow function utilizing default parameters and template literals.
* Added input validation and uppercase text formatting to match user queries with the flight database (`flightGates`).
* Styled the layout cleanly using CSS and structured all markup with semantic HTML tags.

## Author
* **Name:** Web Developer / IT Intern
* **Organization:** Islamabad International Airport (IIAP)